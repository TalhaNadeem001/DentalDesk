# SQLModel models for reminder
# from sqlmodel import SQLModel, Field
#
# class Reminder(SQLModel, table=True):
#     __tablename__ = "reminder"
#     id: int | None = Field(default=None, primary_key=True)
